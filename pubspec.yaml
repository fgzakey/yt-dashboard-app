name: yt_dashboard_app
description: Android companion app for the YT Dashboard — same backend, same database.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  flutter_markdown: ^0.7.3

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
